import Routes from 'Routes';

describe("Routes - ", function () {
	beforeEach(function () {
		//spyOn(can.route, 'attr');
		//can.route.attr.and.callFake(function () { });
		//
		//jasmine.clock().install();
	});

	//it("should be able to set the routes attribute", function (done) {
	//	var promise = Routes.setRoute('testRoute', true);
	//	expect(can.route.attr).not.toHaveBeenCalledWith('testRoute', true);
	//});
	//it("should be able to reset the routes attribute", function (done) {
	//	var promise = Routes.resetRoute();
	//	expect(can.route.attr).not.toHaveBeenCalledWith('', true);
	//});

});
